Service: Player
Types: bool, int

	Observators: 
		command: [Player] → CommandData 
	
	Constructors: 
		init: int → [Player] # a voir pour le int en paramètre
			init(n) requires n ∈ { 1, 2 } ∧ ∄ p : Player 
		
	Operators: 
		step: [Player] × int × c → [Player] 
			pre step(k,c) requires k >= 0 ∧ k <= 255 ∧ c != null # Cékoica ??
		getInput: [Player] × int × c → CommandData # Héça ?
		
	Observations: 
		[invariant]: 
			//
		[init]: 
			//
		[step]: 
			
