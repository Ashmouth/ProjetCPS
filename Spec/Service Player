 Service: Player
Types: bool, int

	Observators: 
		command: [Player] → CommandData 
	
	Constructors: 
		init: int → [Player] # a voir pour le int en paramètre
			init(n) requires n ∈ { 1, 2 } ∧ ∄ p : Player 
		
	Operators: 
		getCommand: [Player] → CommandData # Héça ?
		addInput: [Player] × int -> [Player]
		clearInput: [Player] × int -> [Player]
		
	Observations: 
		[invariant]: 
			//
		[init]: 
			//
		[getCommand]:
		
		[addInput]:
		
		[clearInput]:
			
